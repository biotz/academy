services:
  docusaurus:
    build:
      context: .
    ports:
      - mode: ingress
        target: 3000
        published: 3001
        protocol: tcp
    volumes:
      - '.:/app'
    command:
      - bash
      - -c
      - "npm install --maxsockets 1 && npm start"
